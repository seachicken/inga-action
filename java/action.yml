name: 'Inga'
description: 'Analysis for java'
inputs:
  root-path:
    description: ''
  exclude:
    description: ''
  diff:
    description: ''
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.root-path }}
    - ${{ inputs.exclude }}
    - ${{ inputs.diff }}
